# 芸窗 Yunchuang

离线优先的个人阅读器。本文件只收录本项目特有、且已出现过歧义或即将投入使用的领域术语；通用编程概念不在此列。

## Language

### 排版

**行盒**：
正文经排版后的一行，承载该行覆盖的原文字符范围与占用高度。是排版内核的基本输出单位。
_Avoid_: 行、line、文本行

**页盒**：
一页所容纳的连续行盒集合。由行盒聚合而成，不独立参与排版计算。
_Avoid_: 页面、page、屏

**分页切分点**：
相邻两页交界处的原文字符偏移。可由行盒推导，不是排版的原生产物。
_Avoid_: offset、断点、页偏移

**正文顶部留白**：
用户可在阅读偏好中调节的正文首行上方空白，属于审美偏好。
_Avoid_: 顶部边距、top padding

**顶部工具栏遮挡**：
沉浸式工具栏浮层覆盖正文的高度，是设备与界面决定的物理事实，恒定预留、不随工具栏显隐变化。与「正文顶部留白」相加才是正文的实际起始位置。
_Avoid_: 顶部 padding、安全区、状态栏高度

**度量器**：
向排版内核提供「给定文本与样式在给定宽度下占多高」的能力。内核只依赖这个抽象，不关心背后是引擎测量还是字体表解析。
_Avoid_: measurer、字体度量、TextPainter

### 阅读位置

**全书进度**：
章节索引与章内滚动比例合成的 0–1 值，供书架进度条与持久化共用。不等于「已读章节数 ÷ 总章节数」。
_Avoid_: 百分比、阅读进度、completion

**阅读定位**：
跨搜索结果、笔记、书签、AI 引用与目录链接的统一定位协议，以字符偏移或页码为主、上下文匹配为回退。
_Avoid_: 锚点、position、坐标

### 书架组织

**书架**：
用户自建的书籍分组，一本书可同时属于多个书架，删除书架不删除书籍。
_Avoid_: 分类、标签、文件夹、collection

**系列**：
书籍自带的作品序列归属，带卷序，一本书至多属于一个系列。与「书架」是两套独立机制，不可互相替代。
_Avoid_: 丛书、合集、套装

**阅读状态**：
未读、阅读中、已读完、暂停四种。默认由阅读进度推导，用户手动指定后转为覆盖值。
_Avoid_: 书籍状态、进度状态

### AI 边界

**防剧透级别**：
决定 AI 工具能读到哪里的三档设置——仅限已读范围、越界前询问、允许全书。由本地代码在工具执行层强制，不依赖提示词约束。
_Avoid_: 剧透模式、spoiler 开关

**已读边界**：
当前阅读位置换算出的正文可访问上限。AI 工具的检索与摘录结果都在这条线之前裁剪。
_Avoid_: 阅读上限、进度边界
